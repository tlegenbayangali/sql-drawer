# SQL Drawer

SQL Drawer — это веб-приложение для визуального проектирования и редактирования схем баз данных. Оно позволяет пользователям создавать таблицы, определять столбцы и устанавливать связи между таблицами с помощью интуитивно понятного drag-and-drop интерфейса.

## 🚀 Начало работы

Эти инструкции помогут вам запустить копию проекта на вашем локальном компьютере для разработки и тестирования.

### Необходимые условия

Для работы проекта вам понадобятся:

- [Node.js](https://nodejs.org/en/) (версия 18.x или выше)
- [npm](https://www.npmjs.com/) (обычно поставляется с Node.js)

### 🔧 Установка

1.  **Клонируйте репозиторий:**

    ```sh
    git clone https://github.com/tlegenbayangali/sql-drawer
    cd sql-drawer
    ```

2.  **Установите зависимости:**
    Выполните команду в корневой директории проекта для установки всех необходимых пакетов.

    ```sh
    npm install
    ```

3.  **Настройте переменные окружения:**
    Скопируйте файл `.env.example`, чтобы создать свой файл `.env` с переменными окружения.

    *   **Для Windows (CMD или PowerShell):**
        ```sh
        copy .env.example .env
        ```
    *   **Для macOS и Linux:**
        ```sh
        cp .env.example .env
        ```

4.  **Настройте базу данных:**
    Проект использует Prisma для работы с базой данных SQLite. Выполните следующую команду, чтобы применить миграции и сгенерировать Prisma Client.

    ```sh
    npx prisma migrate dev
    ```

    Эта команда создаст или обновит файл базы данных `prisma/dev.db` в соответствии со схемой.

5.  **Запустите сервер для разработки:**
    Теперь вы можете запустить локальный сервер разработки.

    ```sh
    npm run dev
    ```

6.  **Откройте приложение:**
    Откройте ваш браузер и перейдите по адресу [http://localhost:3000](http://localhost:3000), чтобы увидеть приложение.

## 🛠️ Основные скрипты

- `npm run dev`: запуск сервера для разработки.
- `npm run build`: сборка проекта для продакшена.
- `npm run start`: запуск собранного проекта.
- `npm run lint`: проверка кода с помощью ESLint.

## 💻 Технологии

- **Фреймворк:** [Next.js](https://nextjs.org/)
- **Язык:** [TypeScript](https://www.typescriptlang.org/)
- **ORM:** [Prisma](https://www.prisma.io/)
- **База данных:** SQLite
- **UI:** [React](https://react.dev/), [shadcn/ui](https://ui.shadcn.com/), [Tailwind CSS](https://tailwindcss.com/)
- **Визуализация диаграмм:** [React Flow](https://reactflow.dev/)
